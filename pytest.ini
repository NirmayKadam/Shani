[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests (no external services needed)
    integration: Integration tests (needs Redis, Postgres, or live APIs)
    live: Tests that hit external APIs (NSE, yfinance)
